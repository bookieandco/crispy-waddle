import type { RigAnimationAdapter,RigAnimationArtifact,RigAnimationInput } from './studio-rig-animation'
export interface RigWorkerClient{post(path:string,body:unknown):Promise<unknown>}
export function createRigWorkerAdapter(client:RigWorkerClient,endpoint='/v1/animate'):RigAnimationAdapter{return{name:'rig-worker',async animate(input:RigAnimationInput):Promise<RigAnimationArtifact>{const r=await client.post(endpoint,{characterAssetId:input.characterAssetId,trackingArtifactId:input.trackingArtifactId,approvedTrackIds:input.tracks.map(t=>t.trackId),channels:input.channels,continuityRef:input.continuityRef}) as Record<string,unknown>;if(typeof r.artifactId!=='string'||typeof r.rigAssetId!=='string'||typeof r.animationAssetId!=='string'||typeof r.frameStart!=='number'||typeof r.frameEnd!=='number'||!Array.isArray(r.evidenceIds)||!r.evidenceIds.every(x=>typeof x==='string'))throw new Error('Rig worker returned invalid response');if(r.trackingArtifactId!==input.trackingArtifactId||r.continuityRef!==input.continuityRef)throw new Error('Rig worker changed governed lineage');return{artifactId:r.artifactId,rigAssetId:r.rigAssetId,animationAssetId:r.animationAssetId,frameStart:r.frameStart,frameEnd:r.frameEnd,evidenceIds:r.evidenceIds as string[],provider:'rig-worker'}}}}
