export interface DetectorCommissioningMetrics{realFrames:number;playerRecall:number;officialPrecision:number;ballRecall:number;modelReceipts:number;artifactVerified:boolean;manualDetections:number;}export function certifyDetectorCommissioning(x:DetectorCommissioningMetrics){const checks={realFrames:x.realFrames>0,players:x.playerRecall>=.9,officials:x.officialPrecision>=.8,ball:x.ballRecall>=.8,receipts:x.modelReceipts>0,artifact:x.artifactVerified,noManual:x.manualDetections===0};const failures=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);return Object.freeze({passed:!failures.length,checks:Object.freeze(checks),failures:Object.freeze(failures)});}
