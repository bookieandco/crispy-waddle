export interface ActionLabelCase{truth:string;predicted:string;}export function benchmarkNBAActions(x:readonly ActionLabelCase[]){const labels=[...new Set(x.flatMap(a=>[a.truth,a.predicted]))],perLabel=labels.map(label=>{const tp=x.filter(a=>a.truth===label&&a.predicted===label).length,fp=x.filter(a=>a.truth!==label&&a.predicted===label).length,fn=x.filter(a=>a.truth===label&&a.predicted!==label).length,p=tp/(tp+fp||1),r=tp/(tp+fn||1);return{label,precision:p,recall:r,f1:2*p*r/(p+r||1)};});return Object.freeze({sampleSize:x.length,accuracy:x.filter(a=>a.truth===a.predicted).length/(x.length||1),perLabel:Object.freeze(perLabel)});}
