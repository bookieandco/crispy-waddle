export interface DetectionCase{truth:number;predicted:number;matched:number;}export function benchmarkNBADetection(x:readonly DetectionCase[]){const tp=x.reduce((s,a)=>s+a.matched,0),fp=x.reduce((s,a)=>s+Math.max(0,a.predicted-a.matched),0),fn=x.reduce((s,a)=>s+Math.max(0,a.truth-a.matched),0),precision=tp/(tp+fp||1),recall=tp/(tp+fn||1);return Object.freeze({precision,recall,f1:2*precision*recall/(precision+recall||1)});}
