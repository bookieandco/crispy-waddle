export interface NBAIDClipTrial{videoSha256:string;startMs:number;endMs:number;decodedFrames:number;personDetections:number;tracks:number;jerseyCrops:number;manualIdentityLabelsInjected:number;}export function certifyNBAIDClip(x:NBAIDClipTrial){return /^[a-f0-9]{64}$/i.test(x.videoSha256)&&x.endMs>x.startMs&&x.decodedFrames>0&&x.personDetections>0&&x.tracks>0&&x.jerseyCrops>0&&x.manualIdentityLabelsInjected===0;}