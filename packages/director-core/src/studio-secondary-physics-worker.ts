import type { SecondaryPhysicsAdapter,SecondaryPhysicsArtifact,SecondaryPhysicsInput } from './studio-secondary-physics'
export interface PhysicsWorkerClient{post(path:string,body:unknown):Promise<unknown>}
export function createSecondaryPhysicsWorkerAdapter(client:PhysicsWorkerClient,endpoint='/v1/simulate'):SecondaryPhysicsAdapter{return{name:'physics-worker',async simulate(input:SecondaryPhysicsInput):Promise<SecondaryPhysicsArtifact>{const r=await client.post(endpoint,input) as Record<string,unknown>;if(typeof r.artifactId!=='string'||typeof r.simulatedAnimationAssetId!=='string'||typeof r.frameStart!=='number'||typeof r.frameEnd!=='number'||!Array.isArray(r.evidenceIds)||!r.evidenceIds.every(x=>typeof x==='string'))throw new Error('Physics worker returned invalid response');if(r.rigAssetId!==input.rigAssetId||r.animationAssetId!==input.animationAssetId||r.continuityRef!==input.continuityRef)throw new Error('Physics worker changed governed lineage');return{artifactId:r.artifactId,simulatedAnimationAssetId:r.simulatedAnimationAssetId,frameStart:r.frameStart,frameEnd:r.frameEnd,evidenceIds:r.evidenceIds as string[],provider:'physics-worker'}}}}
