import {test} from 'node:test';import assert from 'node:assert/strict';import {RepositoryRetrievalAuthorizer} from './production-retrieval-authorization.js';
test('memory authorization fails closed for another actor',async()=>{const repo:any={async getById(actor:string){if(actor==='owner')return{id:'m'};throw new Error('unauthorized')}};const a=new RepositoryRetrievalAuthorizer(repo);assert.equal(await a.authorize({principal:{actorId:'other'},taskId:'t',evidenceId:'memory:m',sourceKind:'memory',sourceId:'m'}),'deny')});
test('unknown source kinds deny until concrete ownership policy exists',async()=>{const a=new RepositoryRetrievalAuthorizer({} as any);assert.equal(await a.authorize({principal:{actorId:'u'},taskId:'t',evidenceId:'k',sourceKind:'knowledge',sourceId:'k'}),'deny')});
