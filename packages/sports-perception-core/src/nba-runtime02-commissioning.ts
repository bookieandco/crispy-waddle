export interface NBARuntime02Receipt{modelId:'basketball-ec2xx/1';frameCount:number;inferenceReceiptCount:number;detectionCount:number;personDetectionCount:number;receiptRootHash:string;secretLeaked:boolean;manualDetections:number;}export function certifyNBARuntime02(x:NBARuntime02Receipt){return Object.freeze({passed:x.modelId==='basketball-ec2xx/1'&&x.frameCount>0&&x.inferenceReceiptCount===x.frameCount&&x.detectionCount>0&&x.personDetectionCount>0&&/^[a-f0-9]{64}$/i.test(x.receiptRootHash)&&!x.secretLeaked&&x.manualDetections===0});}