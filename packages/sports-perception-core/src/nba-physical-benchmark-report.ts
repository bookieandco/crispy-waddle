export interface PhysicalBenchmarkMetric{name:string;actual:number;threshold:number;direction:'MIN'|'MAX';}export function evaluatePhysicalBenchmark(metrics:readonly PhysicalBenchmarkMetric[]){const results=metrics.map(m=>Object.freeze({...m,passed:m.direction==='MIN'?m.actual>=m.threshold:m.actual<=m.threshold}));return Object.freeze({passed:results.every(r=>r.passed),results:Object.freeze(results)});}
