import type { NBAEventGameState } from './nba-event-state-machine.js';export interface NBAClockAccuracy{periodMatch:boolean;clockErrorSeconds:number;shotClockErrorSeconds:number;}export function auditNBAClockAccuracy(a:NBAEventGameState,t:NBAEventGameState):NBAClockAccuracy{return Object.freeze({periodMatch:a.period===t.period,clockErrorSeconds:Math.abs(a.periodSecondsRemaining-t.periodSecondsRemaining),shotClockErrorSeconds:Math.abs(a.shotClockSeconds-t.shotClockSeconds)});}
