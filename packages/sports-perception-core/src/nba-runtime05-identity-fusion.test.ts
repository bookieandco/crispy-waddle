import assert from'node:assert/strict';import{describe,it}from'node:test';import{buildNBAPlayerIdCrosswalk}from'./nba-player-id-crosswalk.js';import{fusePhysicalNBAIdentity}from'./nba-physical-identity-fusion.js';import{certifyNBARuntime05}from'./nba-runtime05-identity-fusion.js';describe('NBA-RUNTIME.05',()=>{it('crosswalks only unique evidence-backed matches',()=>{const r=[{playerId:'rg1',name:'Stephen Curry',jerseyNumber:'30',position:'G',height:'',weight:0,age:0,team:'Golden State Warriors',experience:0,preDraftTeam:'',draftStatus:'',nationality:'',sourceUrl:'x',sourceArchiveSha256:'a'.repeat(64)}];const o=[{nbaPlayerId:'201939',name:'Stephen Curry',teamId:'1610612744',team:'Golden State Warriors',jersey:'30',receipt:{endpoint:'CommonPlayerInfo' as const,endpointVersion:'v1',playerId:'201939',observedAt:'x',responseSha256:'b'.repeat(64)}}];assert.equal(buildNBAPlayerIdCrosswalk(r,o)[0]?.nbaPlayerId,'201939')});it('does not resolve weak physical evidence',()=>assert.notEqual(fusePhysicalNBAIdentity('track-17',[{playerId:'201939',roster:true,jerseyScore:.1,reidScore:.1,lineupScore:1,teamScore:1,evidenceIds:['e']}]).status,'RESOLVED'));it('rejects manual identity edits',()=>assert.equal(certifyNBARuntime05({historicalRosterHash:'a'.repeat(64),officialReceiptCount:10,crosswalkCount:10,physicalTracks:10,candidateTracks:10,fusionTracks:10,resolvedTracks:5,ambiguousTracks:3,unknownTracks:2,resolvedWithoutJerseyOrReid:0,resolvedWithoutEvidence:0,manualIdentityEdits:1,receiptRootHash:'b'.repeat(64)}).passed,false));});