export interface PlayerSpatialSample{playerId:string;frameNumber:number;courtX:number;courtY:number;timestampMs:number;confidence:number;}export function deriveSpatialMotion(a:PlayerSpatialSample,b:PlayerSpatialSample){if(a.playerId!==b.playerId||b.timestampMs<=a.timestampMs)throw new Error('Invalid spatial pair');const dt=(b.timestampMs-a.timestampMs)/1000,dx=b.courtX-a.courtX,dy=b.courtY-a.courtY;return Object.freeze({playerId:a.playerId,speed:Math.hypot(dx,dy)/dt,directionRadians:Math.atan2(dy,dx),from:a,to:b,confidence:Math.min(a.confidence,b.confidence)});}
