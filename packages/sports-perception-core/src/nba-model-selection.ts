import type {PhysicalVisionTask} from './physical-model-manifest.js';export interface VisionModelCandidate{candidateId:string;task:PhysicalVisionTask;upstream:string;revision:string;architecture:string;license:string|'UNKNOWN';weightsAvailable:boolean;weightsHash?:string;runtime:'ONNX'|'PYTORCH'|'PROCESS';basketballSpecific:boolean;notes:readonly string[];}export interface ModelSelectionDecision{task:PhysicalVisionTask;selected?:VisionModelCandidate;blocked:boolean;reasons:readonly string[];}export function selectModelCandidate(task:PhysicalVisionTask,candidates:readonly VisionModelCandidate[]):ModelSelectionDecision{const valid=candidates.filter(x=>x.task===task&&x.license!=='UNKNOWN'&&x.weightsAvailable&&!!x.revision&&!!x.weightsHash);const selected=[...valid].sort((a,b)=>Number(b.basketballSpecific)-Number(a.basketballSpecific))[0];return Object.freeze({task,selected,blocked:!selected,reasons:Object.freeze(selected?[]:['no-candidate-with-verified-license-revision-and-weights'])});}
