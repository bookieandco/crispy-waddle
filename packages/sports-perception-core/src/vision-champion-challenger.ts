export interface VisionCandidateResult{modelVersion:string;games:number;primaryMetric:number;regressions:number;temporalHoldout:boolean;artifactVerified:boolean;}export function selectVisionCandidate(champion:VisionCandidateResult,challenger:VisionCandidateResult,minGames=10){const eligible=challenger.games>=minGames&&challenger.temporalHoldout&&challenger.artifactVerified&&challenger.primaryMetric>champion.primaryMetric&&challenger.regressions<=champion.regressions;return Object.freeze({promote:eligible,champion:eligible?challenger:champion,challenger});}
