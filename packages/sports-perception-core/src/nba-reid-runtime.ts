import type {NBAResolvedTrackIdentity,NBAPlayerIdentityCandidate} from './nba-player-reid.js';import {resolveNBAPlayerTrack} from './nba-player-reid.js';export interface NBAReIDRuntime{candidates(trackId:string):Promise<readonly NBAPlayerIdentityCandidate[]>;}export async function resolveRuntimeTrack(r:NBAReIDRuntime,trackId:string,threshold=.8):Promise<NBAResolvedTrackIdentity>{return resolveNBAPlayerTrack(trackId,await r.candidates(trackId),threshold);}
