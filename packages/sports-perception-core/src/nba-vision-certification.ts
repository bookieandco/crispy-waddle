export interface NBAVisionMetrics{realFootage:boolean;detectionF1:number;ballRecall:number;trackingSwitchesPerTrack:number;reidAccuracy:number;reidCoverage:number;courtErrorFeet:number;scoreAccuracy:number;clockMAE:number;actionAccuracy:number;possessionTeamAccuracy:number;possessionBoundaryMAE:number;tacticalF1:number;autonomousTrial:boolean;evidenceLineage:boolean;}export interface NBAVisionThresholds{detectionF1:number;ballRecall:number;maxTrackingSwitchesPerTrack:number;reidAccuracy:number;reidCoverage:number;maxCourtErrorFeet:number;scoreAccuracy:number;maxClockMAE:number;actionAccuracy:number;possessionTeamAccuracy:number;maxPossessionBoundaryMAE:number;tacticalF1:number;}export function certifyNBAVision(m:NBAVisionMetrics,t:NBAVisionThresholds){const checks={realFootage:m.realFootage,detection:m.detectionF1>=t.detectionF1,ball:m.ballRecall>=t.ballRecall,tracking:m.trackingSwitchesPerTrack<=t.maxTrackingSwitchesPerTrack,reid:m.reidAccuracy>=t.reidAccuracy&&m.reidCoverage>=t.reidCoverage,court:m.courtErrorFeet<=t.maxCourtErrorFeet,score:m.scoreAccuracy>=t.scoreAccuracy,clock:m.clockMAE<=t.maxClockMAE,actions:m.actionAccuracy>=t.actionAccuracy,possessions:m.possessionTeamAccuracy>=t.possessionTeamAccuracy&&m.possessionBoundaryMAE<=t.maxPossessionBoundaryMAE,tactics:m.tacticalF1>=t.tacticalF1,autonomous:m.autonomousTrial,lineage:m.evidenceLineage,noFinancialExecution:true};const failures=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);return Object.freeze({passed:!failures.length,checks:Object.freeze(checks),failures:Object.freeze(failures)});}
