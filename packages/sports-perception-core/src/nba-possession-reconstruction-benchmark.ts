export interface PossessionCase{truthStart:number;truthEnd:number;predictedStart:number;predictedEnd:number;teamCorrect:boolean;}export function benchmarkNBAPossessionReconstruction(x:readonly PossessionCase[]){const n=x.length||1;return Object.freeze({sampleSize:x.length,teamAccuracy:x.filter(a=>a.teamCorrect).length/n,boundaryMAE:x.reduce((s,a)=>s+Math.abs(a.truthStart-a.predictedStart)+Math.abs(a.truthEnd-a.predictedEnd),0)/(2*n)});}
