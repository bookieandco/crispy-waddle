export type SportsObjectClass='PLAYER'|'BALL'|'OFFICIAL'|'BASKET';export interface SportsDetection{detectionId:string;frameNumber:number;className:SportsObjectClass;box:readonly [number,number,number,number];confidence:number;evidenceId:string;}export function validateSportsDetection(d:SportsDetection):void{if(d.confidence<0||d.confidence>1||d.box.some(x=>!Number.isFinite(x)))throw new Error('Invalid sports detection');}
