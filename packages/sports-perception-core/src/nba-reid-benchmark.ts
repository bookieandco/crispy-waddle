export interface ReIDCase{truthPlayerId:string;resolvedPlayerId?:string;status:'RESOLVED'|'AMBIGUOUS'|'UNKNOWN';}export function benchmarkNBAReID(x:readonly ReIDCase[]){const resolved=x.filter(a=>a.status==='RESOLVED'),correct=resolved.filter(a=>a.resolvedPlayerId===a.truthPlayerId).length;return Object.freeze({sampleSize:x.length,resolved:resolved.length,accuracy:correct/(resolved.length||1),coverage:resolved.length/(x.length||1)});}
