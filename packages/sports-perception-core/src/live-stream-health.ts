export interface StreamHealthSample{at:string;ok:boolean;latencyMs?:number;sequence?:number;}export function summarizeStreamHealth(x:readonly StreamHealthSample[]){const ok=x.filter(v=>v.ok),lat=ok.filter(v=>v.latencyMs!=null);return Object.freeze({samples:x.length,availability:x.length?ok.length/x.length:0,meanLatencyMs:lat.length?lat.reduce((s,v)=>s+(v.latencyMs??0),0)/lat.length:undefined,lastSequence:ok.at(-1)?.sequence});}
