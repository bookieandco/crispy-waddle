export interface PhysicalDetectionObservation{observationId:string;frameIndex:number;class:string;classId?:number;confidence:number;box:{cx:number;cy:number;width:number;height:number};modelId:string;sourceImageSha256:string;inferenceResponseSha256:string;}export function validatePhysicalDetection(x:PhysicalDetectionObservation){const hash=(v:string)=>/^[a-f0-9]{64}$/i.test(v);return !!x.observationId&&x.frameIndex>=0&&x.confidence>=0&&x.confidence<=1&&x.box.width>=0&&x.box.height>=0&&hash(x.sourceImageSha256)&&hash(x.inferenceResponseSha256);}