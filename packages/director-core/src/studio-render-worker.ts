import type { StudioRenderAdapter,StudioRenderArtifact,StudioRenderInput } from './studio-render'
export interface RenderWorkerClient{post(path:string,body:unknown):Promise<unknown>}
export function createRenderWorkerAdapter(client:RenderWorkerClient,endpoint='/v1/render'):StudioRenderAdapter{return{name:'render-worker',async render(input:StudioRenderInput):Promise<StudioRenderArtifact>{const r=await client.post(endpoint,input) as Record<string,unknown>;if(typeof r.artifactId!=='string'||typeof r.mediaAssetId!=='string'||typeof r.frameStart!=='number'||typeof r.frameEnd!=='number'||!Array.isArray(r.evidenceIds)||!r.evidenceIds.every(x=>typeof x==='string'))throw new Error('Render worker returned invalid response');for(const k of ['sourceAssetId','compositeAssetId','voiceSyncArtifactId','animationAssetId','physicsAssetId','continuityRef'] as const)if(r[k]!==input[k])throw new Error('Render worker changed governed lineage');return{artifactId:r.artifactId,mediaAssetId:r.mediaAssetId,frameStart:r.frameStart,frameEnd:r.frameEnd,evidenceIds:r.evidenceIds as string[],provider:'render-worker'}}}}
