import type {VisionCompute,VisionInferenceReceipt,VisionModelRuntime} from './vision-model-runtime.js';import type {VisionModelRegistration} from './vision-model-registry.js';export interface PhysicalInferenceBackend<I,O>{run(input:I,compute:VisionCompute):Promise<O>;}export class CommissionedModelAdapter<I,O> implements VisionModelRuntime<I,O>{readonly modelId;readonly modelVersion;constructor(readonly registration:VisionModelRegistration,private readonly backend:PhysicalInferenceBackend<I,O>){this.modelId=registration.modelId;this.modelVersion=registration.version;}async infer(input:I,compute:VisionCompute){if(!this.registration.supportedCompute.includes(compute))throw new Error('Unsupported compute');const startedAt=new Date().toISOString(),output=await this.backend.run(input,compute),finishedAt=new Date().toISOString();const receipt:VisionInferenceReceipt={modelId:this.modelId,modelVersion:this.modelVersion,compute,inputEvidenceIds:[],startedAt,finishedAt};return Object.freeze({output,receipt:Object.freeze(receipt)});}}
