import type { NBATacticalConcept } from './nba-tactical-vocabulary.js';export interface NBATacticalLabel{caseId:string;truth:readonly NBATacticalConcept[];predicted:readonly NBATacticalConcept[];}export function benchmarkNBATactics(cases:readonly NBATacticalLabel[]){let tp=0,fp=0,fn=0;for(const c of cases){const t=new Set(c.truth),p=new Set(c.predicted);for(const x of p)t.has(x)?tp++:fp++;for(const x of t)if(!p.has(x))fn++;}const precision=tp/(tp+fp||1),recall=tp/(tp+fn||1);return Object.freeze({precision,recall,f1:2*precision*recall/(precision+recall||1)});}
