export interface SportsWatchArtifacts{videoProvenance:boolean;frameClock:boolean;detections:number;tracks:number;resolvedPlayers:number;events:number;watcherNotes:number;canonicalReality:boolean;tacticalInferences:number;postGameReport:boolean;evidenceBacklinks:boolean;}export function certifySportsWatch(a:SportsWatchArtifacts){const checks={video:a.videoProvenance,clock:a.frameClock,detections:a.detections>0,tracks:a.tracks>0,identity:a.resolvedPlayers>0,events:a.events>0,notes:a.watcherNotes>0,reality:a.canonicalReality,tactics:a.tacticalInferences>0,report:a.postGameReport,backlinks:a.evidenceBacklinks,noFinancialExecution:true};const failures=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);return Object.freeze({passed:!failures.length,checks:Object.freeze(checks),failures:Object.freeze(failures)});}
