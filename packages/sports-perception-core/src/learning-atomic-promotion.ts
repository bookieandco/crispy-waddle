import type { LearningCertification } from './learning-replay-certification.js';import type { LearningPromotionLedger } from './learning-promotion-ledger.js';
export interface ParameterStore{get(path:string):number;set(path:string,value:number):void;}
export function promoteCertifiedLearning(cert:LearningCertification,store:ParameterStore,ledger:LearningPromotionLedger,recordedAt:string):void{if(!cert.passed||cert.finalRecord.disposition!=='VALIDATED')throw new Error('Only certified validated learning may promote');const path=cert.finalRecord.parameterPath,before=store.get(path),after=before+cert.finalRecord.boundedDelta;try{store.set(path,after);ledger.append({entryId:`promote:${cert.certificationId}`,candidateId:cert.candidateId,certificationId:cert.certificationId,target:cert.finalRecord.target,parameterPath:path,beforeValue:before,afterValue:after,action:'PROMOTE',recordedAt,evidenceIds:cert.finalRecord.evidenceIds,realityStateHash:cert.finalRecord.realityStateHash??'missing'});}catch(error){store.set(path,before);throw error;}}
