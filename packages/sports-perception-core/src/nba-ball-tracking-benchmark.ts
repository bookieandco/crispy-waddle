export interface BallTrackCase{frames:number;visibleFrames:number;detectedFrames:number;identitySwitches:number;}export function benchmarkNBABallTracking(x:readonly BallTrackCase[]){const visible=x.reduce((s,a)=>s+a.visibleFrames,0),det=x.reduce((s,a)=>s+Math.min(a.detectedFrames,a.visibleFrames),0),switches=x.reduce((s,a)=>s+a.identitySwitches,0);return Object.freeze({recall:det/(visible||1),identitySwitches:switches,frames:x.reduce((s,a)=>s+a.frames,0)});}
