export type StreamQualityLevel='high'|'medium'|'low';
export interface HysteresisPolicy { degradeAfterMs?:number; recoverAfterMs?:number; }
export interface HysteresisInput { desired:StreamQualityLevel; nowMs:number; }
export class StreamAdaptationHysteresis { private level:StreamQualityLevel; private candidate?:StreamQualityLevel; private candidateSince?:number; constructor(initial:StreamQualityLevel='high',private readonly policy:HysteresisPolicy={}){this.level=initial;} update(input:HysteresisInput):StreamQualityLevel {if(input.desired===this.level){this.candidate=undefined;this.candidateSince=undefined;return this.level;}const recovery=this.rank(input.desired)>this.rank(this.level);const hold=recovery?(this.policy.recoverAfterMs??5000):(this.policy.degradeAfterMs??1000);if(this.candidate!==input.desired){this.candidate=input.desired;this.candidateSince=input.nowMs;return this.level;}if(input.nowMs-(this.candidateSince??input.nowMs)>=hold){this.level=input.desired;this.candidate=undefined;this.candidateSince=undefined;}return this.level;} private rank(level:StreamQualityLevel):number{return level==='high'?2:level==='medium'?1:0;} getLevel():StreamQualityLevel{return this.level;} }
