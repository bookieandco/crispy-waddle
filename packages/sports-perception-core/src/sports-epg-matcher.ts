export interface SportsScheduleEvent{gameId:string;startsAt:string;home:string;away:string;league:string;}export interface EPGProgram{programId:string;channelId:string;startsAt:string;endsAt:string;title:string;}const norm=(s:string)=>s.toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();export function matchGameToEPG(g:SportsScheduleEvent,p:readonly EPGProgram[]){const h=norm(g.home),a=norm(g.away),start=new Date(g.startsAt).getTime();return [...p].map(x=>{const t=norm(x.title),dt=Math.abs(new Date(x.startsAt).getTime()-start);const score=(t.includes(h)?1:0)+(t.includes(a)?1:0)+(dt<=30*60_000?1:0);return{program:x,score};}).sort((x,y)=>y.score-x.score)[0];}
