export interface TrackObservation{trackId:string;frameNumber:number;x:number;y:number;confidence:number;}export interface TrackContinuity{trackId:string;frames:number;gaps:number;maxGap:number;confidence:number;}export function measureTrackContinuity(x:readonly TrackObservation[]):TrackContinuity[]{const m=new Map<string,TrackObservation[]>();for(const o of x)(m.get(o.trackId)??(m.set(o.trackId,[]),m.get(o.trackId)!)).push(o);return [...m].map(([trackId,a])=>{a.sort((p,q)=>p.frameNumber-q.frameNumber);let gaps=0,maxGap=0;for(let i=1;i<a.length;i++){const g=a[i].frameNumber-a[i-1].frameNumber-1;if(g>0){gaps++;maxGap=Math.max(maxGap,g);}}return Object.freeze({trackId,frames:a.length,gaps,maxGap,confidence:a.reduce((s,v)=>s+v.confidence,0)/a.length});});}
