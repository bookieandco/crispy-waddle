export type CommissioningSubsystem='SOURCE'|'DECODER'|'DETECTION'|'BALL'|'TRACKING'|'REID'|'COURT'|'POSE'|'SCOREBOARD'|'ACTION'|'POSSESSION'|'TACTICAL'|'REALITY'|'EVIDENCE';export interface CommissioningFailure{caseId:string;subsystem:CommissioningSubsystem;metric:string;actual:number|boolean;expected:string;evidenceIds:readonly string[];}export function groupCommissioningFailures(x:readonly CommissioningFailure[]){return Object.freeze(x.reduce<Record<string,number>>((a,f)=>(a[f.subsystem]=(a[f.subsystem]??0)+1,a),{}));}
