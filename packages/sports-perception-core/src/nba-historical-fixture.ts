import type { NBAEvent,NBAEventGameState } from './nba-event-state-machine.js';export interface NBAHistoricalFixture{fixtureId:string;gameId:string;sourceId:string;sourceVersion:string;observedAt:string;initialState:NBAEventGameState;events:readonly NBAEvent[];groundTruthFinalState:NBAEventGameState;}export function validateNBAHistoricalFixture(f:NBAHistoricalFixture):void{if(!f.fixtureId||!f.gameId||!f.sourceId)throw new Error('Historical fixture identity/provenance required');if(f.initialState.gameId!==f.gameId||f.groundTruthFinalState.gameId!==f.gameId)throw new Error('Fixture game identity mismatch');}
