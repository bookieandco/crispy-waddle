export interface TacticalVisionCase{truth:readonly string[];predicted:readonly string[];}export function benchmarkNBATacticalVision(x:readonly TacticalVisionCase[]){let tp=0,fp=0,fn=0;for(const c of x){const t=new Set(c.truth),p=new Set(c.predicted);for(const v of p)t.has(v)?tp++:fp++;for(const v of t)if(!p.has(v))fn++;}const precision=tp/(tp+fp||1),recall=tp/(tp+fn||1);return Object.freeze({precision,recall,f1:2*precision*recall/(precision+recall||1)});}
