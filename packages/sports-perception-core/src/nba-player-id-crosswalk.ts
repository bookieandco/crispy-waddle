import type{NBARosterReference}from'./nba-roster-reference.js';import type{NBAOfficialIdentity}from'./nba-official-identity-adapter.js';export interface NBAPlayerIdCrosswalk{realgmPlayerId:string;nbaPlayerId:string;name:string;team:string;jersey?:string;realgmSourceHash:string;officialResponseHash:string;confidence:number;}const norm=(s:string)=>s.toLowerCase().replace(/[^a-z0-9]/g,'');export function buildNBAPlayerIdCrosswalk(roster:readonly NBARosterReference[],official:readonly NBAOfficialIdentity[]){const out:NBAPlayerIdCrosswalk[]=[];for(const r of roster){const matches=official.filter(o=>norm(o.name)===norm(r.name)&&o.team===r.team&&(!r.jerseyNumber||!o.jersey||r.jerseyNumber===o.jersey));if(matches.length===1){const o=matches[0]!;out.push({realgmPlayerId:r.playerId,nbaPlayerId:o.nbaPlayerId,name:r.name,team:r.team,jersey:r.jerseyNumber,realgmSourceHash:r.sourceArchiveSha256,officialResponseHash:o.receipt.responseSha256,confidence:r.jerseyNumber&&o.jersey?.toString()===r.jerseyNumber?.toString()?.trim()?1:.9});}}return Object.freeze(out);}