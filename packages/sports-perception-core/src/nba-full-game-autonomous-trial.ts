import type {PhysicalWatcherRun} from './physical-watcher-run.js';import {auditPhysicalWatcherRun} from './physical-watcher-run.js';export interface NBAFullGameTrial extends PhysicalWatcherRun{completeGame:boolean;groundTruthHiddenDuringRun:boolean;groundTruthComparedAfter:boolean;periodsCompleted:number;}export function certifyFullGameTrial(x:NBAFullGameTrial){const base=auditPhysicalWatcherRun(x),checks={watcher:base.passed,complete:x.completeGame,blind:x.groundTruthHiddenDuringRun,compared:x.groundTruthComparedAfter,periods:x.periodsCompleted>=4};const failures=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);return Object.freeze({passed:!failures.length,checks:Object.freeze(checks),failures:Object.freeze(failures)});}
