export interface PhysicalWatcherRun{runId:string;gameId:string;videoHash:string;manualEvents:number;manualPossessions:number;manualIdentities:number;manualStats:number;manualTactics:number;decodedFrames:number;modelReceipts:number;canonicalEvents:number;possessions:number;statAttributions:number;watcherNotes:number;postGameReport:boolean;}export function auditPhysicalWatcherRun(x:PhysicalWatcherRun){const manual=x.manualEvents+x.manualPossessions+x.manualIdentities+x.manualStats+x.manualTactics,checks={autonomous:manual===0,frames:x.decodedFrames>0,models:x.modelReceipts>0,events:x.canonicalEvents>0,possessions:x.possessions>0,stats:x.statAttributions>0,notes:x.watcherNotes>0,report:x.postGameReport};const failures=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);return Object.freeze({passed:!failures.length,checks:Object.freeze(checks),failures:Object.freeze(failures)});}
