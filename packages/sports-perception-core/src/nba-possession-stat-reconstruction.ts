import type {NBAPlayerStatAttribution} from './nba-player-stat-attribution.js';export interface ReconstructedPossessionStats{possessionId:string;gameId:string;offenseTeamId:string;startEventId:string;endEventId:string;attributions:readonly NBAPlayerStatAttribution[];confidence:number;evidenceIds:readonly string[];}export function reconstructPossessionStats(x:ReconstructedPossessionStats){if(!x.attributions.length||!x.evidenceIds.length)throw new Error('Possession lacks evidence');return Object.freeze({...x,attributions:Object.freeze([...x.attributions]),evidenceIds:Object.freeze([...x.evidenceIds])});}
