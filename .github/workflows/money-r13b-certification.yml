name: Money R13B Certification

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'packages/money-core/**'
      - 'apps/jhadina-web/src/lib/money/**'
      - 'apps/jhadina-web/src/app/money/**'
      - 'apps/jhadina-web/src/app/api/money/**'
      - '.github/workflows/money-r13b-certification.yml'
      - 'package.json'
      - 'pnpm-workspace.yaml'
      - 'pnpm-lock.yaml'
  push:
    branches:
      - main
    paths:
      - 'packages/money-core/**'
      - 'apps/jhadina-web/src/lib/money/**'
      - 'apps/jhadina-web/src/app/money/**'
      - 'apps/jhadina-web/src/app/api/money/**'
      - '.github/workflows/money-r13b-certification.yml'
      - 'package.json'
      - 'pnpm-workspace.yaml'
      - 'pnpm-lock.yaml'

concurrency:
  group: money-r13b-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  certify:
    name: Frozen install, Money certification, build
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8.15.9
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Frozen install
        run: pnpm install --frozen-lockfile

      - name: Money type-check
        run: pnpm --filter @jhadina/money-core type-check

      - name: R13B authority, concurrency, recovery certification
        run: pnpm --filter @jhadina/money-core test:certification

      - name: Full Money test suite
        run: pnpm --filter @jhadina/money-core test

      - name: Money product-boundary tests
        run: pnpm --filter @jhadina/jhadina-web exec vitest run src/lib/money/command-center-model.test.ts src/lib/money/governed-account-read-runtime.test.ts src/lib/money/live-operations-product-model.test.ts

      - name: Money production surface build
        run: pnpm --filter @jhadina/money-core build
