import assert from 'node:assert/strict';import {describe,it} from 'node:test';import {aggregateJerseyOCR} from './nba-jersey-ocr.js';import {acceptReIDLink} from './nba-reid-continuity.js';import {fusePlayerIdentityV2} from './nba-lineup-identity-fusion-v2.js';import {certifyNBAIdentityBenchmark} from './nba-identity-benchmark.js';describe('NBA-ID.10',()=>{it('aggregates jersey evidence across frames',()=>assert.equal(aggregateJerseyOCR([{frame:1,value:'30',confidence:.9,quality:.9},{frame:2,value:'30',confidence:.9,quality:.9}]).status,'RESOLVED'));it('rejects ReID with contradictory jersey',()=>assert.equal(acceptReIDLink({fromTrack:'a',toTrack:'b',embedding:.99,temporal:.99,teamCompatible:true,jerseyCompatible:false}).accepted,false));it('does not resolve weak identity',()=>assert.notEqual(fusePlayerIdentityV2([{playerId:'x',roster:.55,jersey:.4,reid:.5,lineup:.5,team:.9}]).status,'RESOLVED'));it('refuses empirical certification without hidden benchmark evidence',()=>assert.equal(certifyNBAIdentityBenchmark({tracks:0,groundTruthTracks:0,correctResolved:0,falseResolved:0,unknown:0,ambiguous:0,teamCorrect:0,teamTotal:0,jerseyCorrect:0,jerseyReadable:0,reidCorrectLinks:0,reidLinks:0,hiddenGroundTruth:false,manualHelpDuringTrial:false}).passed,false));});