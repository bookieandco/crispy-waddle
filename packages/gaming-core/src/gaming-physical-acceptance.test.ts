import {describe,expect,it} from 'vitest';import {G28_ACCEPTANCE_MATRIX,evaluateG28Case} from './gaming-physical-acceptance.js';import {G28_FAILURE_DRILLS,evaluateG28Drill,evaluateG28Soak} from './gaming-physical-drills.js';
describe('G28.2-.11 physical acceptance',()=>{it('requires actual hardware, minimum samples, all requirements and zero orphans',()=>{const c=G28_ACCEPTANCE_MATRIX[0]!;const sample={pathId:c.pathId,sampleId:'s',capturedAtMs:1,jhadinaCapturedAtMs:2,runtimeReceivedAtMs:3,frameRenderedAtMs:4,displayedAtMs:5,rttMs:0,jitterMs:0,packetLossPercent:0,reconnectCount:0,orphanedResources:0};const receipt={receiptId:'r',pathId:c.pathId,deviceIds:['d'],firmwareVersions:{d:'1'},runtimeVersions:{j:'1'},observedByHardware:false,artifactRefs:[],samples:Array.from({length:20},(_,i)=>({...sample,sampleId:String(i)}))};expect(evaluateG28Case(c,{caseId:c.id,receipt,passedRequirements:c.requirements}).passed).toBe(false);});it('covers 15 destructive failure drills without replay',()=>{expect(G28_FAILURE_DRILLS).toHaveLength(15);expect(evaluateG28Drill({drill:'wifi-loss',hardwareObserved:true,inputReplayCount:0,orphanedResources:0,saveCorruptions:0,recovered:true,artifactRefs:['trace']})).toEqual({passed:true});});it('requires a real four-hour twenty-cycle soak',()=>{expect(evaluateG28Soak({hardwareObserved:false,durationMinutes:240,sessionsStarted:20,sessionsStopped:20,orphanedResources:0,inputIntegrityErrors:0,saveCorruptions:0,unrecoveredCrashes:0,artifactRefs:[]}).passed).toBe(false);});});