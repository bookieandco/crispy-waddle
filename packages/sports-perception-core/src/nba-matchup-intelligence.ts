import type { NBALivePlayerState } from './nba-live-state.js';import {resolveNBALiveSimulationProfile} from './nba-live-state.js';export interface NBALiveMatchupIntel{attackerId:string;defenderId:string;relativeAbility:number;uncertainty:number;}export function resolveNBALiveMatchupIntel(a:NBALivePlayerState,d:NBALivePlayerState):NBALiveMatchupIntel{const ap=resolveNBALiveSimulationProfile(a),dp=resolveNBALiveSimulationProfile(d);return Object.freeze({attackerId:a.playerId,defenderId:d.playerId,relativeAbility:ap.abilityMultiplier/Math.max(.01,dp.abilityMultiplier),uncertainty:(ap.uncertaintyMultiplier+dp.uncertaintyMultiplier)/2});}
