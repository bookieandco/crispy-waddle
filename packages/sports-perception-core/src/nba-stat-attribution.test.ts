import assert from 'node:assert/strict';import {describe,it} from 'node:test';import {attributeScoring} from './nba-scoring-attribution.js';import {NBAStatLedger} from './nba-stat-ledger.js';import {buildPlayerSimulationEvidence} from './nba-stat-simulation-evidence.js';import {gateStatLearning} from './nba-stat-learning-gate.js';describe('NBA player statistical attribution',()=>{it('attributes made threes to player and simulation evidence',()=>{const rows=attributeScoring({gameId:'g',eventId:'e',shooterId:'p1',teamId:'t',points:3,made:true,period:1,gameClockSeconds:600,confidence:.95,evidenceIds:['frame','track','score'],realityStateHash:'r'});assert.equal(rows.find(x=>x.kind==='PTS')?.value,3);assert.ok(rows.some(x=>x.kind==='3PM'));const l=new NBAStatLedger();rows.forEach(x=>l.append(x));assert.equal(l.total('p1','PTS'),3);});it('does not learn unresolved identities',()=>{const rows=[0,1,2].flatMap(i=>attributeScoring({gameId:'g',eventId:'e'+i,shooterId:'UNKNOWN',teamId:'t',points:2,made:true,period:1,gameClockSeconds:600-i,confidence:.99,evidenceIds:['x'],realityStateHash:'r'}));const e=buildPlayerSimulationEvidence(rows,'UNKNOWN','PTS')!;assert.equal(gateStatLearning(e).eligible,false);});});