import type {NBARosterReference} from './nba-roster-reference.js';export interface NBAGameRoster{gameId:string;gameDate:string;teamId:string;team:string;players:readonly NBARosterReference[];sourceHash:string;}export function resolveGameRoster(x:NBAGameRoster){if(!x.gameId||!x.gameDate||!x.teamId||!x.sourceHash)throw new Error('Incomplete game roster provenance');return Object.freeze(x.players.slice());}