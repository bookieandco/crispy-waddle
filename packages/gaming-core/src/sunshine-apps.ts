export interface SunshineRemoteApp {id:string;name:string;cmd?:string;workingDirectory?:string;imagePath?:string;enabled:boolean;}
export interface SunshineAppsClient {listApps(hostId:string,signal?:AbortSignal):Promise<readonly SunshineRemoteApp[]>;}
export class SunshineAppCatalog {constructor(private readonly client:SunshineAppsClient,private readonly requirePaired:(hostId:string)=>Promise<void>){} async list(hostId:string):Promise<readonly SunshineRemoteApp[]>{if(!hostId.trim())throw new Error('Sunshine host id is required');await this.requirePaired(hostId);const apps=await this.client.listApps(hostId);const seen=new Set<string>();return apps.filter(a=>a.enabled&&a.id?.trim()&&a.name?.trim()).map(a=>({...a,id:a.id.trim(),name:a.name.trim()})).filter(a=>{if(seen.has(a.id))return false;seen.add(a.id);return true;});}}
