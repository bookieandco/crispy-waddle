export type VideoSourceKind='UPLOAD'|'LOCAL'|'CAMERA'|'AUTHORIZED_STREAM';export interface SportsVideoSource{videoId:string;gameId:string;kind:VideoSourceKind;uri:string;contentHash:string;startedAt:string;fps:number;durationSeconds?:number;}export function validateSportsVideoSource(v:SportsVideoSource):void{if(!v.videoId||!v.gameId||!v.uri||!v.contentHash)throw new Error('Video source identity/provenance required');if(!(v.fps>0))throw new Error('Video fps must be positive');}
