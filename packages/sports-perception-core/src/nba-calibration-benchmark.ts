export interface NBACalibrationPoint{probability:number;outcome:0|1;}export function benchmarkNBACalibration(x:readonly NBACalibrationPoint[]){if(!x.length)return Object.freeze({sampleSize:0,brier:0,logLoss:0,calibrationGap:0});const eps=1e-12,brier=x.reduce((s,a)=>s+(a.probability-a.outcome)**2,0)/x.length,logLoss=-x.reduce((s,a)=>s+a.outcome*Math.log(Math.max(eps,a.probability))+(1-a.outcome)*Math.log(Math.max(eps,1-a.probability)),0)/x.length,p=x.reduce((s,a)=>s+a.probability,0)/x.length,o=x.reduce((s,a)=>s+a.outcome,0)/x.length;return Object.freeze({sampleSize:x.length,brier,logLoss,calibrationGap:p-o});}
