import {describe,expect,it,vi} from 'vitest'
import {createRigWorkerAdapter} from './studio-rig-animation-worker'
const input={characterAssetId:'muppet',trackingArtifactId:'sam2:a',tracks:[{trackId:'t',class:'character' as const,instanceId:'i',frameStart:0,frameEnd:10,annotations:[],source:'human' as const,confidence:1,approved:true}],channels:['body','head'] as const,animationPlan:{version:1 as const,narrativeGoal:'readable head turn',primaryAction:'turn head',method:'pose-to-pose' as const,poseHierarchy:{keys:['front','turn','settle']},timing:{fps:24,exposure:'twos' as const}},continuityRef:'dna'}
describe('rig worker',()=>{it('preserves tracking lineage and forwards governed animation direction',async()=>{const post=vi.fn(async()=>({artifactId:'a',rigAssetId:'r',animationAssetId:'x',trackingArtifactId:'sam2:a',continuityRef:'dna',frameStart:0,frameEnd:10,evidenceIds:['motion']}));expect((await createRigWorkerAdapter({post}).animate({...input,channels:[...input.channels]})).rigAssetId).toBe('r');expect(post).toHaveBeenCalledWith('/v1/animate',expect.objectContaining({animationPlan:expect.objectContaining({method:'pose-to-pose'})}))});it('rejects lineage substitution',async()=>{const a=createRigWorkerAdapter({post:vi.fn(async()=>({artifactId:'a',rigAssetId:'r',animationAssetId:'x',trackingArtifactId:'other',continuityRef:'dna',frameStart:0,frameEnd:10,evidenceIds:[]}))});await expect(a.animate({...input,channels:[...input.channels]})).rejects.toThrow('changed governed lineage')})})
