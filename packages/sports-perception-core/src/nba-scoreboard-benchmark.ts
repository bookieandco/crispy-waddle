export interface ScoreboardCase{scoreCorrect:boolean;periodCorrect:boolean;clockErrorSeconds:number;shotClockErrorSeconds:number;}export function benchmarkNBAScoreboard(x:readonly ScoreboardCase[]){const n=x.length||1;return Object.freeze({sampleSize:x.length,scoreAccuracy:x.filter(a=>a.scoreCorrect).length/n,periodAccuracy:x.filter(a=>a.periodCorrect).length/n,clockMAE:x.reduce((s,a)=>s+Math.abs(a.clockErrorSeconds),0)/n,shotClockMAE:x.reduce((s,a)=>s+Math.abs(a.shotClockErrorSeconds),0)/n});}
