import type {PhysicalModelManifest} from './physical-model-manifest.js';import {validatePhysicalModelManifest} from './physical-model-manifest.js';export interface ModelDeploymentReceipt{deploymentId:string;modelId:string;version:string;artifactSha256:string;runtime:string;deployedAt:string;healthCheckPassed:boolean;}export function deploymentReceipt(m:PhysicalModelManifest,deploymentId:string,healthCheckPassed:boolean):ModelDeploymentReceipt{validatePhysicalModelManifest(m);return Object.freeze({deploymentId,modelId:m.modelId,version:m.version,artifactSha256:m.artifactSha256,runtime:m.runtime,deployedAt:new Date().toISOString(),healthCheckPassed});}
