import type { NBAActionCandidate } from './nba-action-recognition.js';export interface NBAPlaySequence{sequenceId:string;actions:readonly NBAActionCandidate[];confidence:number;evidenceIds:readonly string[];}export function reconstructNBAPlaySequence(id:string,a:readonly NBAActionCandidate[]):NBAPlaySequence{const x=[...a].sort((l,r)=>l.startFrame-r.startFrame);return Object.freeze({sequenceId:id,actions:Object.freeze(x),confidence:x.length?x.reduce((s,v)=>s+v.confidence,0)/x.length:0,evidenceIds:Object.freeze([...new Set(x.flatMap(v=>v.evidenceIds))])});}
