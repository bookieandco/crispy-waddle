export interface NBAIdentityBenchmark{tracks:number;groundTruthTracks:number;correctResolved:number;falseResolved:number;unknown:number;ambiguous:number;teamCorrect:number;teamTotal:number;jerseyCorrect:number;jerseyReadable:number;reidCorrectLinks:number;reidLinks:number;hiddenGroundTruth:boolean;manualHelpDuringTrial:boolean;}export function certifyNBAIdentityBenchmark(x:NBAIdentityBenchmark){const precision=x.correctResolved/Math.max(1,x.correctResolved+x.falseResolved),recall=x.correctResolved/Math.max(1,x.groundTruthTracks),teamAccuracy=x.teamCorrect/Math.max(1,x.teamTotal),jerseyAccuracy=x.jerseyCorrect/Math.max(1,x.jerseyReadable),reidAccuracy=x.reidCorrectLinks/Math.max(1,x.reidLinks);const checks={corpus:x.tracks>=50&&x.groundTruthTracks>=50,hidden:x.hiddenGroundTruth,noManualHelp:!x.manualHelpDuringTrial,precision:precision>=.98,recall:recall>=.9,falseIdentity:x.falseResolved===0,team:teamAccuracy>=.98,jersey:jerseyAccuracy>=.9,reid:reidAccuracy>=.95};return Object.freeze({passed:Object.values(checks).every(Boolean),metrics:Object.freeze({precision,recall,teamAccuracy,jerseyAccuracy,reidAccuracy}),checks:Object.freeze(checks)});}