import type { NBATacticalConcept } from './nba-tactical-vocabulary.js';import type { NBAPlaySequence } from './nba-temporal-play-reconstruction.js';export interface NBATacticalVisionInference{concept:NBATacticalConcept;classification:'INFERENCE'|'HYPOTHESIS';confidence:number;evidenceIds:readonly string[];}export function inferNBATactic(sequence:NBAPlaySequence,concept:NBATacticalConcept,confidence:number):NBATacticalVisionInference{return Object.freeze({concept,classification:confidence>=.8?'INFERENCE':'HYPOTHESIS',confidence:Math.max(0,Math.min(1,confidence)),evidenceIds:sequence.evidenceIds});}
