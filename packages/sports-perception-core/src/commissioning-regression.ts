import type {CommissioningFailure} from './commissioning-failure-attribution.js';export interface CommissioningRegressionCase{regressionId:string;failure:CommissioningFailure;videoRef:string;groundTruthRef:string;createdAt:string;}export function regressionFromFailure(f:CommissioningFailure,videoRef:string,groundTruthRef:string):CommissioningRegressionCase{return Object.freeze({regressionId:`vision:${f.caseId}:${f.subsystem}:${f.metric}`,failure:f,videoRef,groundTruthRef,createdAt:new Date().toISOString()});}
