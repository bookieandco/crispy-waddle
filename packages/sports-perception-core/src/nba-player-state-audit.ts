import type { NBAEventGameState } from './nba-event-state-machine.js';export interface NBAPlayerStateAudit{playerId:string;foulDelta:number;fatigueDelta:number;eligibleMatch:boolean;}export function auditNBAPlayerStates(a:NBAEventGameState,t:NBAEventGameState):readonly NBAPlayerStateAudit[]{return Object.freeze(Object.keys(t.players).sort().map(id=>{const x=a.players[id],y=t.players[id];return Object.freeze({playerId:id,foulDelta:(x?.personalFouls??0)-y.personalFouls,fatigueDelta:(x?.fatigue??0)-y.fatigue,eligibleMatch:x?.eligible===y.eligible});}));}
