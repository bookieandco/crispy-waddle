import type { NBAEventGameState } from './nba-event-state-machine.js';const ids=(s:NBAEventGameState)=>[...(s.offenseLineup?.onCourt??[]),...(s.defenseLineup?.onCourt??[])].map(x=>x.playerId).sort();export function nbaLineupAccuracy(a:NBAEventGameState,t:NBAEventGameState):number{const x=ids(a),y=ids(t);if(!y.length)return x.length?0:1;return y.filter(id=>x.includes(id)).length/y.length;}
