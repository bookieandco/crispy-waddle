import{describe,expect,it,vi}from'vitest';import{RemoteLaunchAuthorizer}from'./remote-launch-authorization.js';
const base={game:{id:'g',title:'Game',platform:'pc' as const,contentUri:'moonlight://h/a',metadata:{hostId:'h'}},app:{id:'h:a',hostId:'h',name:'Game',kind:'game' as const,launchTarget:'a'},playClass:'action' as const,quality:{rttMs:20,jitterMs:2,packetLossPercent:0,inputLatencyMs:20},qualityPolicy:{maxRttMs:60,maxJitterMs:12,maxPacketLossPercent:1,maxInputLatencyMs:45},hostCapabilities:{video:['h264','hevc'] as const,audio:['stereo'] as const,hdr:true,maxFps:120,maxWidth:3840,maxHeight:2160},deviceCapabilities:{video:['h264','hevc'] as const,hdr:true,maxFps:120,maxWidth:1920,maxHeight:1080}};
describe('RemoteLaunchAuthorizer',()=>{it('authorizes a trusted compatible target',async()=>{const a=new RemoteLaunchAuthorizer({requirePaired:vi.fn(async()=>{})} as any);expect((await a.authorize(base)).allowed).toBe(true);});it('collects all blocking reasons',async()=>{const a=new RemoteLaunchAuthorizer({requirePaired:async()=>{throw new Error()}} as any);const d=await a.authorize({...base,game:{...base.game,metadata:{hostId:'other'}},quality:{...base.quality,rttMs:100},streamRequirement:{video:'av1'}});expect(d.allowed).toBe(false);expect(d.reasons.length).toBeGreaterThanOrEqual(3);});});
