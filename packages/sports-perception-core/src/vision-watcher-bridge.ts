import type { PerceptionObservation } from './live-game-watcher.js';import type { NBAActionCandidate } from './nba-action-recognition.js';export function actionToWatcherObservation(gameId:string,sourceId:string,a:NBAActionCandidate,observedAt:string):PerceptionObservation{return Object.freeze({observationId:`vision:${a.actionId}`,eventId:gameId,sourceId,observedAt,sourceTimestamp:a.startFrame,kind:'PLAY_CLASSIFICATION',text:a.action,subjectIds:Object.freeze([...a.subjectTrackIds]),confidence:a.confidence,evidenceIds:Object.freeze([...a.evidenceIds])});}
