import type {PlayerSimulationEvidence} from './nba-stat-simulation-evidence.js';export interface StatLearningDecision{eligible:boolean;weight:number;reasons:readonly string[];}export function gateStatLearning(e:PlayerSimulationEvidence,minConfidence=.8,minSamples=3):StatLearningDecision{const reasons:string[]=[];if(e.playerId==='UNKNOWN'||e.playerId==='AMBIGUOUS')reasons.push('identity-unresolved');if(e.confidence<minConfidence)reasons.push('low-confidence');if(e.sampleCount<minSamples)reasons.push('insufficient-sample');if(!e.evidenceIds.length||!e.realityStateHashes.length)reasons.push('missing-lineage');return Object.freeze({eligible:!reasons.length,weight:!reasons.length?Math.min(1,e.confidence*Math.log2(e.sampleCount+1)/3):0,reasons:Object.freeze(reasons)});}
