import{test}from'node:test';import assert from'node:assert/strict';import{AudioPerceptionExtractor,VideoPerceptionExtractor}from'./media-extractor-adapters.js';
const asset:any={id:'a',actorId:'u',modality:'video',mediaType:'video/mp4',assetRef:'opaque',privacyClass:'internal',createdAt:'2026-01-01T00:00:00Z',status:'registered'};
test('video adapter converts backend observations to immutable asset evidence',async()=>{const x=new VideoPerceptionExtractor({async extract(){return{observations:[{kind:'frame',summary:'boxer throws jab'}]}}});const o=await x.extract({asset});assert.equal(o.evidence[0].id,'asset:a:frame:0');assert.equal(o.evidence[0].immutable,true)});
test('audio adapter uses same governed boundary',async()=>{const x=new AudioPerceptionExtractor({async extract(){return{observations:[{kind:'transcript',summary:'hello'}],uncertainty:['speaker unknown']}}});const o=await x.extract({...asset,asset:{...asset,modality:'audio',mediaType:'audio/mpeg'}});assert.equal(o.evidence[0].source,'perception:audio');assert.deepEqual(o.uncertainty,['speaker unknown'])});
