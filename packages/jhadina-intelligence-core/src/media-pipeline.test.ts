import{test}from'node:test';import assert from'node:assert/strict';import{GovernedMediaPipeline}from'./media-pipeline.js';
const asset:any={id:'asset_1',actorId:'u',modality:'video',mediaType:'video/mp4',assetRef:'s',privacyClass:'internal',createdAt:'2026-01-01T00:00:00Z',status:'registered'};
test('composes extraction then subsystem routing',async()=>{const calls:string[]=[];const p=new GovernedMediaPipeline({async extract(i:any){calls.push('extract');return{assetId:i.asset.id,evidence:[{id:'asset:asset_1:frame:0',source:'p',observedAt:i.asset.createdAt,summary:'frame',immutable:true}],uncertainty:[]}}},{async route(a:any){calls.push('route');return{assetId:a.id,routes:[{subsystem:'sports-intelligence',reason:'intent',confidence:.9}],requiresHumanSelection:false}}});const o=await p.process({asset,intent:'boxing'});assert.deepEqual(calls,['extract','route']);assert.equal(o.evidence[0].id,'asset:asset_1:frame:0');assert.equal(o.routing.routes[0].subsystem,'sports-intelligence')});
test('pipeline exposes evidence/routing, not execution',()=>{const p:any=new GovernedMediaPipeline({},{}) ;assert.equal(p.execute,undefined);assert.equal(p.approve,undefined)});
