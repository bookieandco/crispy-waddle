export interface NBAReferenceEvidence{evidenceId:string;source:'ROSTER_REFERENCE';playerId:string;observedAt:string;sourceHash:string;confidence:number;}export function rosterReferenceEvidence(playerId:string,sourceHash:string,observedAt:string):NBAReferenceEvidence{if(!/^[a-f0-9]{64}$/i.test(sourceHash))throw new Error('Invalid source hash');return Object.freeze({evidenceId:`roster:${sourceHash}:${playerId}`,source:'ROSTER_REFERENCE',playerId,observedAt,sourceHash,confidence:.55});}