import assert from 'node:assert/strict';import {describe,it} from 'node:test';
import {LearningPromotionLedger} from './learning-promotion-ledger.js';import {learningSnapshotAsOf} from './learning-temporal-snapshot.js';import {evaluateShadowLearning} from './shadow-learning.js';import {detectLearningDrift} from './learning-drift-watcher.js';import {CrossSportLearningRegistry} from './cross-sport-learning.js';
describe('B-70.13.57-.64 production learning foundation',()=>{it('preserves immutable promotion history and temporal snapshots',()=>{const l=new LearningPromotionLedger();l.append({entryId:'e1',candidateId:'c1',certificationId:'cert1',target:'PLAYER_STATE',parameterPath:'p',beforeValue:1,afterValue:1.02,action:'PROMOTE',recordedAt:'2026-09-20T10:00:00Z',evidenceIds:['x'],realityStateHash:'r1'});assert.equal(learningSnapshotAsOf(l.snapshot(),'2026-09-20T11:00:00Z').values.p,1.02);});it('requires enough shadow evidence',()=>{const x=Array.from({length:20},()=>({production:.7,shadow:.8,actual:1}));assert.equal(evaluateShadowLearning(x).improved,true);});it('detects regression drift',()=>{assert.equal(detectLearningDrift([{metric:'brier',baseline:.1,current:.2,tolerance:.05}]).drifted,true);});it('enforces cross-sport learning governance',()=>{const r=new CrossSportLearningRegistry();assert.throws(()=>r.register({sport:'NBA',allowedTargets:['PLAYER_STATE'],maximumAbsoluteDelta:.1,minimumValidations:2}));r.register({sport:'NBA',allowedTargets:['PLAYER_STATE'],maximumAbsoluteDelta:.05,minimumValidations:2});assert.equal(r.policy('NBA')?.minimumValidations,2);});});
