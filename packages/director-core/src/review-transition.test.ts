import {describe,expect,it} from 'vitest';import {planDirectorReviewTransition} from './review-transition';import type {MediaReviewDecisionRecord} from './media-review-lifecycle';
const base:MediaReviewDecisionRecord={id:'d',projectId:'p',runId:'r',gateId:'g',generationStageId:'gen',generationStageVersion:4,reviewStageId:'rev',reviewStageVersion:2,assetId:'a',generationJobId:'job',decision:'approved',evidenceIds:['e'],decidedBy:'u',decidedAt:'t',provenance:{projectId:'p',storyboardBoardIds:['b'],storyboardVersion:1,generationStageId:'gen',generationStageVersion:4,generationJobId:'job'}};
describe('review transition planner',()=>{it('approves exact reviewed versions without a rerun',()=>{const x=planDirectorReviewTransition(base);expect(x.nextReviewStatus).toBe('approved');expect(x.successorGenerationVersion).toBeUndefined();});it('changes requested stale reviewed lineage and deterministically increments generation',()=>{const x=planDirectorReviewTransition({...base,decision:'changes_requested'});expect(x.nextGenerationStatus).toBe('stale');expect(x.successorGenerationVersion).toBe(5);});it('rejection uses the same deterministic successor version',()=>{expect(planDirectorReviewTransition({...base,decision:'rejected'}).successorGenerationVersion).toBe(5);});});
