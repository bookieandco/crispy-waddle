import type {SportsSourceAuthorization} from './sports-source-policy.js';export interface SportsWatchSource{sourceId:string;kind:'IPTV'|'SUBSCRIPTION_DEEPLINK'|'RECORDING'|'UPLOAD'|'CAMERA';uri:string;authorization:SportsSourceAuthorization;channelId?:string;priority:number;}export function resolveSportsWatchSource(x:readonly SportsWatchSource[]):SportsWatchSource|undefined{return [...x].filter(s=>s.authorization!=='UNKNOWN'&&s.authorization!=='DISALLOWED').sort((a,b)=>b.priority-a.priority)[0];}
