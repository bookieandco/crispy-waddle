export interface NBABlindGame{complete:boolean;periods:number;groundTruthHiddenUntilFinal:boolean;manualEvents:number;manualPossessions:number;manualIdentities:number;manualStats:number;manualTactics:number;postGameCompared:boolean;}export function certifyNBABlindGame(x:NBABlindGame){const c={complete:x.complete,periods:x.periods>=4,blind:x.groundTruthHiddenUntilFinal,autonomous:[x.manualEvents,x.manualPossessions,x.manualIdentities,x.manualStats,x.manualTactics].every(v=>v===0),compared:x.postGameCompared};return Object.freeze({passed:Object.values(c).every(Boolean),checks:Object.freeze(c)});}