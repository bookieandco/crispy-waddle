export type AudioSemanticRole = 'dialogue' | 'music' | 'sfx' | 'foley' | 'ambience' | 'voiceover';
export type AudioEvent = { id: string; assetId: string; role: AudioSemanticRole; startSeconds: number; durationSeconds: number; gainDb?: number; pan?: number; fadeInSeconds?: number; fadeOutSeconds?: number; sourceStartSeconds?: number; metadata?: Record<string, unknown> };
export type FoleyGenerationInput = { videoAssetId: string; videoUri: string; prompt?: string; startSeconds?: number; endSeconds?: number; characterId?: string; environmentId?: string };
export type FoleyCandidate = { assetId: string; provider: string; durationSeconds: number; metadata: Record<string, unknown> };
export interface FoleyProvider { readonly id: string; generate(input: FoleyGenerationInput): Promise<FoleyCandidate[]>; }
export class UnconfiguredFoleyProvider implements FoleyProvider { readonly id = 'unconfigured-foley'; async generate(input: FoleyGenerationInput): Promise<FoleyCandidate[]> { return [{ assetId: `foley-pending-${input.videoAssetId}`, provider: this.id, durationSeconds: Math.max(0, (input.endSeconds ?? 0) - (input.startSeconds ?? 0)), metadata: { generated: false, providerConfigured: false } }]; } }
export function createFoleyEvent(input: { id: string; candidate: FoleyCandidate; startSeconds: number; role?: 'foley' | 'sfx'; metadata?: Record<string, unknown> }): AudioEvent { return { id: input.id, assetId: input.candidate.assetId, role: input.role ?? 'foley', startSeconds: input.startSeconds, durationSeconds: input.candidate.durationSeconds, metadata: { provider: input.candidate.provider, ...input.candidate.metadata, ...input.metadata } }; }
