export interface NBAPlayerStatAccuracy{games:number;points:number;fg:number;three:number;ft:number;assists:number;rebounds:number;steals:number;blocks:number;turnovers:number;fouls:number;minutes:number;unresolvedLeakage:number;}export function certifyNBAPlayerStats(x:NBAPlayerStatAccuracy){const vals=[x.points,x.fg,x.three,x.ft,x.assists,x.rebounds,x.steals,x.blocks,x.turnovers,x.fouls,x.minutes],c={games:x.games>=3,accuracy:vals.every(v=>v>=.9),identity:x.unresolvedLeakage===0};return Object.freeze({passed:Object.values(c).every(Boolean),checks:Object.freeze(c)});}