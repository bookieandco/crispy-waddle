import type { LiveIntelligenceState } from './live-intelligence-state.js';import type { EventReconstruction } from './live-event-reconstruction.js';import type { PostGameIntelligenceReport } from './post-game-intelligence-report.js';export interface LiveIntelligenceCertification{passed:boolean;checks:Readonly<Record<string,boolean>>;failures:readonly string[];}
export function certifyLiveIntelligence(state:LiveIntelligenceState,reconstruction:EventReconstruction,report:PostGameIntelligenceReport):LiveIntelligenceCertification{const checks={identity:state.gameId===report.gameId,reality:Boolean(state.realityStateHash),evidence:state.evidenceIds.length>0,events:reconstruction.events.every(x=>x.gameId===state.gameId),conflictsPreserved:reconstruction.conflicts.every(x=>x.eventIds.length>1),hypothesisAccounting:report.supported+report.contradicted<=report.hypotheses.length,noFinancialExecution:true};const failures=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);return Object.freeze({passed:failures.length===0,checks:Object.freeze(checks),failures:Object.freeze(failures)});}
