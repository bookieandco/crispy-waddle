import{describe,expect,it}from'vitest';import{adaptVideoPreservingInput,G43_G50_PHASES,GamingRouteLearningLedger,UniversalSaveVault,assertOneButtonPlayAuthorization}from'./gaming-g43-g50-production.js';describe('G43-G50 production',()=>{it('selects measured healthy route',()=>{const l=new GamingRouteLearningLedger();l.record({gameId:'g',runtimeId:'local',deviceId:'d',displayId:'tv',controllerProfileId:'p',networkClass:'offline',compatible:true,measured:true,inputLatencyMs:4,frameTimeMs:16,crashed:false,saveRoundTripPassed:true});expect(l.best('g')?.runtimeId).toBe('local');});it('degrades video before healthy input',()=>{expect(adaptVideoPreservingInput({resolution:'2160p',fps:60,bitrateMbps:40},true,true).resolution).toBe('1440p');});it('preserves known good save and conflicts',()=>{const v=new UniversalSaveVault();v.put({gameId:'g',revision:1,digest:'a',knownGood:true,createdAtMs:1});expect(()=>v.put({gameId:'g',revision:1,digest:'b',knownGood:false,createdAtMs:2})).toThrow(/conflict/);expect(v.latestGood('g')?.digest).toBe('a');});it('keeps Play authorization gated and closes eight phases',()=>{expect(()=>assertOneButtonPlayAuthorization({gameId:'g',authorized:false})).toThrow();expect(G43_G50_PHASES).toHaveLength(8);});});