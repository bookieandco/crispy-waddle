import {describe,expect,it,vi} from 'vitest'
import {createSecondaryPhysicsWorkerAdapter} from './studio-secondary-physics-worker'
const input={characterAssetId:'muppet',rigAssetId:'rig',animationAssetId:'anim',layers:[{id:'coat',material:'puppet-fabric' as const,attachment:'torso',stiffness:.4,damping:.6,gravityScale:1}],frameStart:0,frameEnd:20,continuityRef:'dna'}
describe('physics worker',()=>{it('preserves rig lineage',async()=>{const post=vi.fn(async()=>({artifactId:'p',simulatedAnimationAssetId:'sim',rigAssetId:'rig',animationAssetId:'anim',continuityRef:'dna',frameStart:0,frameEnd:20,evidenceIds:['stable']}));expect((await createSecondaryPhysicsWorkerAdapter({post}).simulate(input)).simulatedAnimationAssetId).toBe('sim')});it('rejects substitution',async()=>{const a=createSecondaryPhysicsWorkerAdapter({post:vi.fn(async()=>({artifactId:'p',simulatedAnimationAssetId:'sim',rigAssetId:'other',animationAssetId:'anim',continuityRef:'dna',frameStart:0,frameEnd:20,evidenceIds:[]}))});await expect(a.simulate(input)).rejects.toThrow('changed governed lineage')})})
