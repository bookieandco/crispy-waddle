export interface LineupCandidate{playerId:string;team:string;active:boolean;confidence:number;evidenceId:string;}export function lineupScore(playerId:string,team:string,lineup:readonly LineupCandidate[]){const same=lineup.filter(x=>x.team===team&&x.playerId===playerId);if(!same.length)return 0;const active=Math.max(...same.filter(x=>x.active).map(x=>x.confidence),0);const inactive=Math.max(...same.filter(x=>!x.active).map(x=>x.confidence),0);return Math.max(0,Math.min(1,active-inactive));}export function validateLineup(team:string,lineup:readonly LineupCandidate[]){return lineup.filter(x=>x.team===team&&x.active&&x.confidence>=.5).length<=5;}