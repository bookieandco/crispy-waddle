export interface NBAPlayerIdentityCandidate{playerId:string;teamId:string;jerseyNumber?:string;confidence:number;evidenceIds:readonly string[];}export interface NBAResolvedTrackIdentity{trackId:string;status:'RESOLVED'|'AMBIGUOUS'|'UNKNOWN';playerId?:string;confidence:number;evidenceIds:readonly string[];}export function resolveNBAPlayerTrack(trackId:string,c:readonly NBAPlayerIdentityCandidate[],threshold=.8):NBAResolvedTrackIdentity{const x=[...c].sort((a,b)=>b.confidence-a.confidence),top=x[0],second=x[1];if(!top||top.confidence<threshold)return Object.freeze({trackId,status:'UNKNOWN',confidence:top?.confidence??0,evidenceIds:Object.freeze(top?.evidenceIds??[])});if(second&&top.confidence-second.confidence<.1)return Object.freeze({trackId,status:'AMBIGUOUS',confidence:top.confidence,evidenceIds:Object.freeze([...new Set([...top.evidenceIds,...second.evidenceIds])])});return Object.freeze({trackId,status:'RESOLVED',playerId:top.playerId,confidence:top.confidence,evidenceIds:Object.freeze([...top.evidenceIds])});}
