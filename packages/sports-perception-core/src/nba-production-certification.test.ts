import assert from 'node:assert/strict';import {describe,it} from 'node:test';import {resolveNBAFoulPossession} from './nba-possession-rules.js';import {advanceNBAClock} from './nba-clock.js';import {auditNBAHistoricalBacktest} from './nba-historical-backtest.js';import {certifyNBAProduction} from './nba-production-certification.js';import {isNBATacticalConcept} from './nba-tactical-vocabulary.js';
describe('B-70.15 NBA production vertical',()=>{it('resolves foul possession semantics',()=>{assert.equal(resolveNBAFoulPossession({foulKind:'NON_SHOOTING',inBonus:false,freeThrows:0}),'RETAIN_POSSESSION');assert.equal(resolveNBAFoulPossession({foulKind:'SHOOTING',inBonus:true,freeThrows:2,finalFreeThrowMade:true}),'CHANGE_POSSESSION');});it('detects shot clock expiry deterministically',()=>{assert.equal(advanceNBAClock(100,5,5).shotClockViolation,true);});it('detects temporal leakage in historical backtests',()=>{assert.equal(auditNBAHistoricalBacktest([{predictionId:'p',cutoff:'2026-01-01T00:00:00Z',evidenceObservedAt:['2026-01-01T00:00:01Z']}]).passed,false);});it('recognizes NBA tactical vocabulary',()=>{assert.equal(isNBATacticalConcept('DROP'),true);});it('requires every production gate',()=>{const c=certifyNBAProduction({passed:true,firstHash:'a',secondHash:'a'},{passed:true,leakingPredictionIds:[]},true,true);assert.equal(c.passed,true);});});
