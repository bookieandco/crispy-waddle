export interface NBATacticalMetrics{games:number;observationPrecision:number;changePrecision:number;hypothesisValidationRate:number;observationInferenceSeparation:boolean;evidenceLineage:boolean;}export function certifyNBATacticalIntelligence(x:NBATacticalMetrics){const c={games:x.games>=5,observations:x.observationPrecision>=.9,changes:x.changePrecision>=.8,hypotheses:x.hypothesisValidationRate>=.6,separation:x.observationInferenceSeparation,lineage:x.evidenceLineage};return Object.freeze({passed:Object.values(c).every(Boolean),checks:Object.freeze(c)});}