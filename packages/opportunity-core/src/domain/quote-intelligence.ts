import type { ProviderQuote } from './provider-quote.js'
export type NormalizedQuote={providerId:string;quoteId:string;amount:number;currency:string;requirementIds:string[];evidenceRef:string;validUntil?:string;expired:boolean}
export function normalizeProviderQuotes(quotes:ProviderQuote[],now=new Date().toISOString()){const t=Date.parse(now);const normalized:NormalizedQuote[]=quotes.filter(q=>q.status==='accepted_for_model'&&q.quotedAmount!==undefined&&q.quoteEvidenceRef).map(q=>({providerId:q.providerId,quoteId:q.id,amount:q.quotedAmount!,currency:q.currency,requirementIds:q.requirementIds,evidenceRef:q.quoteEvidenceRef!,validUntil:q.validUntil,expired:!!q.validUntil&&Date.parse(q.validUntil)<=t}));return{quotes:normalized,usable:normalized.filter(q=>!q.expired),expired:normalized.filter(q=>q.expired),pricingCommitmentAuthorized:false as const,paymentAuthorized:false as const}}
