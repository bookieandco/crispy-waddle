import assert from 'node:assert/strict';import {describe,it} from 'node:test';import {VisionModelRegistry} from './vision-model-registry.js';import {benchmarkNBAReID} from './nba-reid-benchmark.js';import {auditNBAAutonomousWatchTrial} from './nba-autonomous-watch-trial.js';import {certifyNBAVision} from './nba-vision-certification.js';
describe('B-70.19 NBA vision validation',()=>{it('pins model artifacts',()=>{const r=new VisionModelRegistry();r.register({modelId:'det',version:'1',artifactHash:'abc',task:'player',inputResolution:[640,640],supportedCompute:['GPU'],provenance:'test'});assert.equal(r.get('det','1')?.artifactHash,'abc');});it('measures ReID coverage separately from accuracy',()=>{const r=benchmarkNBAReID([{truthPlayerId:'a',resolvedPlayerId:'a',status:'RESOLVED'},{truthPlayerId:'b',status:'UNKNOWN'}]);assert.equal(r.accuracy,1);assert.equal(r.coverage,.5);});it('forbids manual event injection in autonomous trial',()=>assert.equal(auditNBAAutonomousWatchTrial({trialId:'t',videoId:'v',manualEventInputs:1,decodedFrames:1,canonicalEvents:1,possessions:1,watcherNotes:1,postGameReport:true,groundTruthCompared:true}).passed,false));it('certifies only measured real-footage performance',()=>{const t={detectionF1:.9,ballRecall:.8,maxTrackingSwitchesPerTrack:.1,reidAccuracy:.9,reidCoverage:.8,maxCourtErrorFeet:2,scoreAccuracy:.99,maxClockMAE:2,actionAccuracy:.85,possessionTeamAccuracy:.95,maxPossessionBoundaryMAE:2,tacticalF1:.8};const m={realFootage:true,detectionF1:.95,ballRecall:.9,trackingSwitchesPerTrack:.05,reidAccuracy:.95,reidCoverage:.9,courtErrorFeet:1,scoreAccuracy:1,clockMAE:1,actionAccuracy:.9,possessionTeamAccuracy:.98,possessionBoundaryMAE:1,tacticalF1:.85,autonomousTrial:true,evidenceLineage:true};assert.equal(certifyNBAVision(m,t).passed,true);});});
